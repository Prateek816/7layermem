# 7layermem — AI Agent Memory System

# ── Core (required) ──────────────────────────────────────────────────────────
langchain-core>=0.2.0
langchain-chroma>=0.1.0
langchain-huggingface>=0.1.0
langchain-community>=0.2.0
sentence-transformers>=2.6.0
chromadb>=0.5.0

# ── Graph DB (optional, for Neo4j entity memory) ────────────────────────────
neo4j>=5.0.0
langchain-groq>=0.1.0

# ── RAG (optional, for hybrid retrieval) ─────────────────────────────────────
rank-bm25>=0.2.2
nltk>=3.8.0
numpy>=1.24.0
